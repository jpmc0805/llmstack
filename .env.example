# Copy this file to .env and fill in your own values
# cp .env.example .env

# Secret key used by Open-WebUI to sign session tokens.
# Generate a strong random value, for example:
#   openssl rand -hex 32
WEBUI_SECRET_KEY=change-this-to-a-strong-random-secret
